# Database
POSTGRES_PASSWORD=your_secure_password_here

# Security Keys (generate with: openssl rand -hex 32)
ENCRYPTION_KEY=
JWT_SECRET=

# Application URL
FRONTEND_URL=http://your-vm-ip-or-domain

# Optional: SMTP Configuration
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SENDER_EMAIL=
